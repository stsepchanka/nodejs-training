const actions = ['encode', 'decode'];

const lengthABC = 26;

const startCode = {
  uppercase: 'A'.charCodeAt(0),
  lowercase: 'a'.charCodeAt(0)
};

module.exports = {
  actions,
  lengthABC,
  startCode
};
